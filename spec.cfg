cl_interp 0.1

bind shift "demo_togglepause"
bind z "demo_timescale 0.5"
bind x "demo_timescale 1"
bind c "demo_timescale 4"
bind q "+movedown"
bind e "+moveup"
bind alt "+speed"
bind r "hud_reloadscheme"

bind mouse2 "spec_prev"

bind KP_DOWNARROW +lookdown
bind KP_LEFTARROW +left
bind KP_RIGHTARROW +right
bind KP_UPARROW +lookup
